
<% if @experiment.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@experiment.errors.count, "error") %> prohibited this proposal from being saved:</h2>

    <ul>
    <% @experiment.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
